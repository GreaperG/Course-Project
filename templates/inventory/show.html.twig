{% extends 'base.html.twig' %}

{% block title %}Inventory{% endblock %}

{% block body %}
    <h1>Inventory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ inventory.id }}</td>
            </tr>
            <tr>
                <th>items</th>
                <td>{{ inventory.items|length }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_inventory_index') }}">Back to List</a>

    <a href="{{ path('app_inventory_items', {'inventoryId': inventory.id}) }}">Add Items</a>

    <a href="{{ path('app_inventory_edit', {'id': inventory.id}) }}">Edit Inventory</a>


    {{ include('inventory/_delete_form.html.twig') }}
{% endblock %}
